import styled from "styled-components";

export const StyledPageHeader = styled.h1`
  position: absolute;
  top: 22px;
  left: 50%;
  transform: translateX(-50%);
  font-size: 15px;
  font-weight: 600;
  text-align: center;
`;
